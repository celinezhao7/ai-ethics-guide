streamlit
python-dotenv
# The Google generative AI SDK is not consistently published under a
# single package name on PyPI. Do NOT include a package name here to
# avoid install-time failures.
# To enable the generative AI functionality, install the provider SDK
# manually. Common options include:
#   pip install google-generative-ai
#   pip install google-generativeai
# If your environment doesn't have a published package, follow the
# provider's install instructions.